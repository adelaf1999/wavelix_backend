if @country != nil
    node(:country) { @country }
end

if @currency != nil
    node(:currency) {  @currency }
end

if @currencies != nil
    node(:currencies) { @currencies }
end

if @countries != nil
    node(:countries) { @countries }
end